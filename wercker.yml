test:
  box: node:6.4.0
  steps:
    - npm-install
    - script:
      name: compile
      code: npm run compile
    - npm-test
    - script:
      name: lint
      code: npm run lint

coverage:
  box: node:6.4.0
  steps:
    - npm-install
    - script:
      name: compile
      code: npm run compile
    - npm-test
    - script:
      name: coverage
      code: |
        # npm run report-coverage
        echo Broken for now

release:
  box: node:6.4.0
  steps:
    - npm-install
    - script:
      name: compile
      code: npm run compile
    - script:
      name: build
      code: npm run build
    - script:
      name: npm publish
      code: npm publish
